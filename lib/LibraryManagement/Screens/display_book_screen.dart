import 'package:flutter/material.dart';

class DisplayBookScreen extends StatefulWidget {
  const DisplayBookScreen({Key? key}) : super(key: key);

  @override
  _DisplayBookScreenState createState() => _DisplayBookScreenState();
}

class _DisplayBookScreenState extends State<DisplayBookScreen> {
  @override
  Widget build(BuildContext context) {
    return ListView();
  }
}
